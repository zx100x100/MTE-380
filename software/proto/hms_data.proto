syntax = "proto3";

message HmsData {
  enum Error {
    LOW_BATTERY = 0;
    OFF_TRACK = 1; // robot off track
  }
  string errorInfo = 1;
  float batteryVoltage = 2;
  uint32 nCells = 3;
  // Error error = 4;
}
