syntax = "proto3";

message TeleopCmd{
  float left_power = 1;
  float right_power = 2;
}
  
enum RunState{
  E_STOP = 0;
  STOP = 1;
  RUN = 2;
}

message RobotState{
  RunState run_state = 1;
}

message Command{
  // TeleopCmd teleop_cmd = 1;
  // RobotState robot_state = 2;
  float test = 1;
}
